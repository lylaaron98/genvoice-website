"use client";

import HomeContent from '../../components/HomeContent';

const ProfilePage = () => {
    return <HomeContent />; // Render HomeContent as the profile page
};

export default ProfilePage;
